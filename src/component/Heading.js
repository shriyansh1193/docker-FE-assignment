import { Card, Stack } from "react-bootstrap";
import Badge from "react-bootstrap/Badge";
import Button from "react-bootstrap/Button";

function Heading() {
  return (
    <div>
      <Card>
        <Card.Header>Books Management App</Card.Header>
      </Card>
    </div>
  );
}

export default Heading;
